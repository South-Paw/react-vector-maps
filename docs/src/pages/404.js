import React from 'react';
import { Layout } from '../components/Layout';

const seo = {
  title: 'Page not found',
};

const NotFoundPage = () => <Layout seo={seo}>404 lol</Layout>;

export default NotFoundPage;
