import React from 'react';

import { Layout } from '../components/Layout';

const seo = {
  title: 'Examples',
};

const ExamplesPage = () => (
  <Layout seo={seo}>
    <h2>Examples</h2>
    <p>todo</p>
  </Layout>
);

export default ExamplesPage;
