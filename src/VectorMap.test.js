const { name } = require('../package.json');

import VectorMap from './index';
import world from '../maps/json/world.json';

describe(name, () => {
  it('todo', () => {
    console.log('todo');
  });
});
